<?php

class {{ migrationClass }} extends AbstractMigration
{
    public function up()
    {
        ${{ table }} = $this->table('{{ table }}');

        {{#each columns}}
        ${{ this.table }}->addColumn('{{ this.name }}', '{{ this.type.type }}' {{#if this.options}}, [
            {{#each this.options}}
                '{{@key}}' => {{this}}
            {{/each}}
        ]{{/if}});

        {{/each}}

        ${{ table }}->save();
    }

    public function down()
    {
        $this->dropTable('{{ table }}');
    }
}